{
  "name": "grunt-assetser",
  "description": "Inject assets files (js, css) into HTML files.",
  "version": "0.1.3",
  "author": {
    "name": "Denis Malinochkin",
    "url": "http://canonium.com"
  },
  "repository": "mrmlnc/grunt-assetser",
  "licenses": "MIT",
  "engines": {
    "node": ">= 0.10.0"
  },
  "scripts": {
    "test": "xo && rimraf tmp && grunt assetser && node test/test.js"
  },
  "dependencies": {
    "detect-indent": "^3.0.1",
    "indent-string": "^1.2.2"
  },
  "devDependencies": {
    "ava": "^0.1.0",
    "grunt": "~0.4.5",
    "rimraf": "^2.4.3",
    "xo": "^0.8.0"
  },
  "peerDependencies": {
    "grunt": "~0.4.5"
  },
  "keywords": [
    "gruntplugin",
    "inject",
    "inline",
    "assets",
    "stylesheets",
    "styles",
    "scripts"
  ],
  "files": [
    "tasks"
  ],
  "xo": {
    "space": true,
    "rules": {
      "object-curly-spacing": [
        2,
        "always"
      ],
      "space-before-function-paren": 0
    },
    "envs": [
      "node"
    ]
  }
}
